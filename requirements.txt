# DocGuard AI — core dependencies (initial setup only)
# ML, web, OCR, and deployment libraries are added per research phase.

python-dotenv>=1.0.0,<2.0.0
pyyaml>=6.0,<7.0

# Research / ML development (initial phase — PyTorch installed separately)
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
jupyter>=1.0.0
Pillow>=10.0.0
tqdm>=4.66.0

# Phase 1 — document ingestion, preprocessing, OCR
opencv-python>=4.8.0
pytesseract>=0.3.10
pymupdf>=1.23.0
pytest>=7.4.0

# Phase 2 — document understanding / VLM baseline
# Install separately on CPU-only machines (no CUDA):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# pip install transformers accelerate
